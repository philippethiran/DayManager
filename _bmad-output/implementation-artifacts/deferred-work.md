# Deferred Work — DayManager

Goals deferred from the current quick-dev pass (split chosen by user).

## Google Calendar integration

- Connect a single Google account (read-only calendar events).
- Map meetings to day tasks (attendance) and merge with manual tasks in the day view.
- Clarify UI behavior for calendar-sourced tasks (display-only vs complete/hide) when this work starts.
